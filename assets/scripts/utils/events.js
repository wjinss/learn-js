function click(element, listener) {
  element.addEventListener("click", listener);
}

function mouseLeave(elm, listener) {
  elm.addEventListener("mouseleave", listener);
}
function mouseEnter(elm, listener) {
  elm.addEventListener("mouseenter", listener);
}
